from setuptools import setup

setup(name='binomial_gaussian',
      version='0.1',
      description='Binomial and Gaussian distributions',
      packages=['binomial_gaussian'],
      author='KaalaAadmi'
      zip_safe=False)
